import React from 'react'

const SettingsError = ({error, setError, toggleShowShare}) => {
    return (
        <div>
            <div>Error: {error}</div>
        </div>
    )
}

export default SettingsError
